# Baseline de seguridad y estado real — 23 de agosto de 2026

Auditoría de Fase 0 (`ROADMAP-TERMINACION.md`). Solo lectura: no se aplicó ninguna migración ni se cambió funcionalidad. Ningún secreto real aparece en este documento — donde hacía falta identificar una credencial se describe su forma (nombre de variable, longitud), nunca su valor.

## 1. Secretos en Git — limpio

- Se escaneó el historial completo (`git log --all -p`) buscando patrones de token de Supabase (`sbp_...`), JWT (`eyJ...`), Mercado Pago (`APP_USR-...`) y asignaciones de `SUPABASE_SERVICE_ROLE_KEY`/`MP_ACCESS_TOKEN`. Única coincidencia: el placeholder `MP_ACCESS_TOKEN=your_mercadopago_access_token` en `.env.example` — no es un secreto real.
- Ningún archivo `.env*`, `.mcp.json` o `.cursor/*` fue agregado nunca al historial de Git (búsqueda de archivos creados en cualquier commit de cualquier rama).
- Ramas auditadas: `main`, `feature/mercadopago-payments-backend` (locales y sus remotos en `origin`) — no hay otras.
- `.gitignore` cubre correctamente `.env*`, `.mcp.json`, `.cursor/mcp.json`, `.cursor/*token*`, `.vercel`.

**Conclusión:** no hay secretos comprometidos en el repositorio. El hallazgo previo del roadmap (token de Supabase visible en una captura de pantalla del `.mcp.json` local) sigue siendo válido como motivo para rotar — pero no por una fuga en Git.

## 2. Credenciales locales — pendiente de rotar (acción manual, no la puedo hacer yo)

Archivos locales confirmados como existentes (nunca trackeados por Git):

| Archivo | Contenido (solo forma, no valor) |
| --- | --- |
| `.mcp.json` | Un servidor MCP (`servicasa`) con `url` (61 caracteres) y header `Authorization` (51 caracteres, formato típico `Bearer sbp_...`) |
| `.env.local` | 9 variables: `MP_ACCESS_TOKEN`, `MP_PUBLIC_KEY`, `MP_WEBHOOK_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `VERCEL_AUTOMATION_BYPASS_SECRET`, `VERCEL_OIDC_TOKEN`, `VITE_DEMO_MODE`, `VITE_SUPABASE_ANON_KEY`, `VITE_SUPABASE_URL` |
| `.cursor/mcp.json` | Existe, no inspeccionado en detalle (mismo patrón esperado) |

**Buena noticia:** ninguna variable server-only tiene el prefijo `VITE_*` — la separación cliente/servidor está bien hecha. Solo `VITE_DEMO_MODE`, `VITE_SUPABASE_ANON_KEY` y `VITE_SUPABASE_URL` llegan al bundle del navegador, que es lo esperado.

**Pendiente real (no lo puedo hacer desde acá):** rotar el token de Supabase usado en `.mcp.json`/`.cursor/mcp.json` desde el dashboard de Supabase (Account → Access Tokens), y reemplazar el `.mcp.json` con el token nuevo. Esto requiere acción tuya en el navegador con tu login.

## 3. Conexión MCP a Supabase — sigue sin resolverse

~~El conector Supabase MCP disponible en esta sesión sigue apuntando a un proyecto/organización distinto~~ **Actualizado el mismo 23/8: reconectaste el conector con `sandy722sandy@hotmail.com` y ahora ve `ayszrtieplmqscqtabsu` (ServiCasa) directo.** Todo el inventario de la sección 4 se armó primero a mano (vos corriendo las consultas SQL) y la sección de Advisors de abajo ya se completó con la conexión directa, sin pasos manuales. Este punto de la Fase 0 queda cerrado.

## 4. Inventario de Supabase live

**38 tablas en `public`, todas con RLS activado (0 excepciones).** Lista completa:

`account_invites`, `categories`, `customer_addresses`, `customer_admin_notes`, `customers`, `guest_checkout_drafts`, `materials`, `order_checklist_items`, `order_diagnosis_photos`, `order_events`, `order_materials_used`, `order_notes`, `order_quote_items`, `order_quotes`, `order_signatures`, `order_time_logs`, `payment_transactions`, `price_adjustments_log`, `profiles`, `rubro_matricula_config`, `service_categories`, `service_orders`, `service_rubros`, `services`, `subcategories`, `system_settings`, `technician_applications`, `technician_documents`, `technician_enablement_checklist`, `technician_goals`, `technician_matriculas`, `technician_notifications`, `technician_payment_accounts`, `technician_payout_batches`, `technician_requirements`, `technician_review_history`, `technician_settlements`, `technicians`.

**21 funciones en `public`, 5 son `SECURITY DEFINER`:** `current_user_role`, `get_account_invite`, `handle_new_user`, `is_admin`, `redeem_account_invite`. Las 16 restantes son `security invoker` — set razonable y mínimo, sin candidatos obvios a reducir. Pendiente para la Fase 1: confirmar que las 5 `SECURITY DEFINER` tengan `set search_path = ''` (buena práctica ya usada en los triggers de pago más recientes, no verificado todavía en estas 5 específicas).

**Storage — 4 buckets:** `avatars` (privado), `diagnosis-photos` (privado), `technician-avatars` (**público**), `technician-documents` (privado). El único bucket público es intencional (avatares de técnicos, dato no sensible).

**Extensiones:** al menos `pg_stat_statements` (1.11) instalada; **`pg_cron` NO está instalada.** Esto confirma y explica el hallazgo del roadmap: `release_due_technician_settlements()` no tiene forma de ejecutarse automáticamente porque la infraestructura de Cron ni siquiera existe en esta base — hay que habilitar la extensión antes de programar nada en la Fase 5.

**Migraciones registradas:** solo **17**, todas del 20-21 de agosto (ej. `add_missing_service_type_enum_values`, `guest_checkout_access_token`, `customer_delete_own_cancelled_orders`, `customer_self_registration_and_technician_applications`). **Nada de lo aplicado el 22-23 de agosto está registrado ahí** (categorías/subcategorías, `guest_checkout_drafts`, numeración de clientes/técnicos, columna de provincia, unificación del catálogo de pago directo). Todo eso vive únicamente como archivos sueltos en `supabase/sql/`, aplicados a mano por el SQL Editor. Esta es la brecha central que la Fase 1 tiene que cerrar.

**Cron jobs:** ninguno (la tabla `cron.job` ni siquiera existe, ver extensiones).

### Advisors de seguridad (Supabase Security Advisor, obtenido vía MCP)

| Nivel | Hallazgo | Detalle |
| --- | --- | --- |
| **ERROR** | Vista `SECURITY DEFINER` | `public.technician_public_view` está definida como `SECURITY DEFINER`. **Revisada su definición (23/8):** parece intencional y razonablemente segura — el propio `WHERE` de la vista ya reimplementa la autorización (admin, o el propio técnico, o un cliente con una orden activa asignada a ese técnico) y solo expone campos no sensibles (nombre, especialidad, rating, bio, licencias validadas). El `SECURITY DEFINER` existe porque necesita leer `technician_matriculas` (con RLS más restrictivo) para armar `validated_licenses`. El linter no puede verificar que el `WHERE` sea correcto, así que sigue siendo ERROR automático — pero no parece ser un hueco real. Igual conviene que la Fase 1 lo firme explícitamente (confirmar que no es alcanzable por `anon`, y que el `WHERE` cubre todos los casos) en vez de dejarlo solo como "parece bien". |
| WARN | 4 funciones `SECURITY DEFINER` invocables directo por API | `get_account_invite`, `redeem_account_invite` son llamables por `anon` vía `/rest/v1/rpc/...`; `current_user_role`, `is_admin`, y otra vez `get_account_invite`/`redeem_account_invite` son llamables por `authenticated`. Las dos primeras son intencionales (un invitado sin sesión necesita canjear su invitación). `current_user_role` e `is_admin` son helpers pensados para usarse **dentro** de políticas RLS, no como endpoint público — revisar si conviene revocarles `EXECUTE` a `anon`/`authenticated` directamente. |
| WARN | Protección de contraseñas filtradas desactivada | Supabase Auth no está chequeando contraseñas contra HaveIBeenPwned. Arreglo de un click en el dashboard (Authentication → Policies). |
| INFO | `guest_checkout_drafts` con RLS activado sin policies | **Intencional** — esta tabla la diseñamos así a propósito el 23/8: solo el service-role (`supabaseAdmin`) la toca, nadie más debería poder leerla ni por accidente. No requiere acción. |

### Advisors de performance (Supabase Performance Advisor, obtenido vía MCP)

**91 hallazgos en total**, agrupados:

| Cantidad | Nivel | Tipo | Qué significa |
| ---: | --- | --- | --- |
| 37 | INFO | `unindexed_foreign_keys` | Columnas de foreign key sin índice propio (ej. en `account_invites`, `order_checklist_items`, `order_quotes`, y ~30 más). No urgente con el volumen de datos actual, pero barato de resolver y va a importar cuando crezca. |
| 30 | WARN | `multiple_permissive_policies` | Tablas con más de una política RLS permisiva para el mismo rol/acción (Postgres evalúa todas con OR) — afecta a `categories`, `customer_addresses`, `customers`, `materials`, y ~26 más. Señal de que conviene consolidar políticas en la Fase 1, no solo por performance sino porque hace más difícil auditar quién puede ver qué. |
| 20 | WARN | `auth_rls_initplan` | Políticas RLS que llaman `auth.uid()`/`auth.jwt()` sin envolver en `(select ...)`, por lo que Postgres las reevalúa fila por fila en vez de una sola vez por consulta — afecta a `profiles`, `service_orders`, `order_checklist_items`, `order_time_logs`, y varias más. Patrón conocido y fácil de arreglar (envolver la llamada), pero está repetido en ~20 lugares — vale la pena resolverlo de una vez en la Fase 1 en vez de ir tabla por tabla más adelante. |
| 4 | INFO | `unused_index` | Índices creados pero nunca usados por el planner, en `service_orders`, `technician_goals`, `materials` — candidatos a eliminar, prioridad baja. |

## 5. Hallazgo nuevo, corrige un documento anterior

El informe "Estado de Obra" (artifact) y el roadmap describían el módulo de **Reclamos y Garantías** como "tablas creadas, solo falta conectar la navegación". **Esto es incorrecto contra la base real:** las tablas `support_cases`, `support_case_messages` y `support_case_history` — que `src/lib/supportCases.ts`, `ClaimsTable.tsx` y `NewClaimModal.tsx` esperan encontrar — **no existen** en la lista de 38 tablas live. El archivo `supabase/sql/enable_support_cases.sql` que las crearía nunca se ejecutó contra esta base de datos.

**Impacto en la Fase 2 del roadmap:** antes de "conectar" el módulo de Reclamos a la navegación, primero hay que correr `enable_support_cases.sql` (auditado como cualquier otra migración de la Fase 1) — no es un trabajo de solo interfaz como se pensaba, tiene un paso de base de datos previo no contemplado.

## 6. Inventario de Vercel

- **Producción:** `tecniurbano.online`, proyecto `prj_ngyF5OHqHXcaQxRdwzpVS7zG6bK1`, Node.js **24.x**. Ni la Fase 1 (que pide normalizar a Node 22) ni la documentación vieja (que menciona Node 18) reflejan la realidad — es un tercer valor. Decisión pendiente: ¿pinnear a 22 como dice el roadmap, o aceptar 24.x si ya funciona bien?
- **Dominio:** `tecniurbano.online`, registrado hace 3 días, DNS de terceros.
- **Variables de entorno:** 8 configuradas (`MP_PUBLIC_KEY`, `MP_ACCESS_TOKEN`, `VERCEL_AUTOMATION_BYPASS_SECRET`, `MP_WEBHOOK_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `VITE_DEMO_MODE`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`), todas marcadas "Sensitive" (valor oculto). **Las 8 están configuradas igual en Preview y en Production** — Preview reutiliza las credenciales reales de Mercado Pago y el service-role key de producción. No es urgente ahora, pero es exactamente lo que la Fase 9 tiene que separar más adelante.
- **Despliegues automáticos:** confirmado que un `git push` a la rama dispara un deploy de Preview solo (GitHub integration activa) — comportamiento esperado, no requiere acción.
- **Sin verificar por falta de acceso vía CLI (chequeo manual pendiente en el dashboard):** estado exacto de "Deployment Protection", y la URL/estado configurado del webhook dentro del dashboard de Mercado Pago.

## 7. Checklist de la Fase 0 — resultado

- [x] Confirmar que ningún secreto aparece en Git, ramas remotas, logs o artefactos.
- [x] Inventariar Supabase live (tablas, RLS, funciones, Storage, extensiones, migraciones, Cron).
- [x] Advertencias de seguridad y rendimiento (Supabase Advisors) — completado tras reconectar el MCP.
- [x] Inventariar Vercel (deploys, variables, dominio, Node).
- [x] Crear este informe baseline sin datos secretos.
- [x] Rotar los tokens personales de Supabase usados por MCP — token "claude code" (ya expirado) borrado, uno nuevo generado.
- [x] Conectar Claude Code al proyecto correcto vía OAuth de Supabase MCP — resuelto el mismo 23/8, reconectado con `sandy722sandy@hotmail.com`.
- [ ] Chequeo manual en dashboard: Deployment Protection y webhook de Mercado Pago. Único ítem que sigue pendiente.

## 8. Lista priorizada de diferencias entre lo documentado y la realidad

1. **Reclamos y Garantías no tiene tablas en producción** (contradice el "Estado de Obra" anterior) — impacta directamente el punto de partida de la Fase 2.
2. **Vista `technician_public_view` es `SECURITY DEFINER`** (único ERROR del Security Advisor) — revisar en Fase 1 si es intencional.
3. **Solo 17 migraciones registradas vs. 38 tablas reales y ~2 semanas de scripts sueltos** — confirma y cuantifica la brecha de reproducibilidad que la Fase 1 debe cerrar.
4. **`pg_cron` no instalado** — bloquea por completo la Fase 5 hasta habilitarlo.
5. **20 políticas RLS con el patrón `auth_rls_initplan`** (llaman `auth.uid()` sin envolver en `select`) y **30 con `multiple_permissive_policies`** — deuda de performance/claridad concentrada, vale la pena resolverla de una vez en la Fase 1.
6. **Node 24.x real vs. "18" (docs viejas) vs. "22" (meta del roadmap)** — decisión de versión pendiente, ninguna de las dos referencias anteriores es correcta.
7. **Preview de Vercel usa las mismas credenciales que Production** — no bloquea nada hoy, pero es deuda para la Fase 9.
8. **Leaked Password Protection desactivada en Supabase Auth** — arreglo de un click, sin excusa para no hacerlo ya.

**Resueltos durante esta misma Fase 0** (ya no son diferencias abiertas): credenciales de MCP rotadas, y el conector reconectado al proyecto correcto — ambos completados en esta sesión.
